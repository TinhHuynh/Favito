<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      tools:context="com.huynhtinh1997.favito.MainActivity">
    <item
        android:id="@+id/photo_capture"
        android:icon="@drawable/ic_photo_capture"
        android:title="@string/capture_photo"
        app:showAsAction="always"/>
    <item
        android:id="@+id/video_record"
        android:icon="@drawable/ic_video_record"
        android:title="@string/record_video"
        app:showAsAction="always"/>
    <item
        android:id="@+id/login_logout"
        android:icon="@drawable/com_facebook_button_icon"
        android:title="@string/log_in_out"
        app:showAsAction="never|withText"/>
</menu>
